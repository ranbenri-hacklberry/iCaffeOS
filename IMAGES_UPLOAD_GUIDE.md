# 📸 מדריך להעלאת וניהול תמונות תפריט במערכת iCaffeOS

מדריך זה מסביר כיצד להעלות תמונות למשקאות ומאכלים בתפריט המערכת, איפה הן נשמרות פיזית, וכיצד לוודא שהן נטענות בצורה תקינה וחסכונית בזיכרון בכל הטאבלטים והמכשירים בעסק.

---

## 🛠️ אפשרות 1: העלאה אוטומטית דרך ממשק הניהול (מומלץ)

הדרך הפשוטה ביותר להעלות תמונות היא דרך ממשק ניהול הפריטים באפליקציה או בדפדפן:

1. היכנס ל**פורטל הניהול** באייפד או בדפדפן במחשב.
2. ניגש לעריכת פריט תפריט קיים (או יצירת פריט חדש).
3. לחץ על **העלאת תמונה** (Upload Image).
4. בחר את קובץ התמונה מהמכשיר שלך.
5. המערכת תבצע אוטומטית:
   * העלאה של התמונה לענן המאובטח (Supabase Storage).
   * שמירת כתובת תמונה ייחודית בבסיס הנתונים.
   * סנכרון אוטומטי של התמונה לטאבלט הקצה המקומי.

---

## 💻 אפשרות 2: העלאה ידנית ישירות לשרת (Mac Mini)

אם יש לך קבצי תמונות מוכנים ואתה רוצה להעלות אותם בבת אחת ישירות לשרת המקומי ללא שימוש בממשק:

### שרת המק מיני (Mac Mini) - מיקום פיזי של הקבצים:
כל תמונות התפריט המקומיות נשמרות בתיקייה הבאה בשרת:
📁 `/Users/icaffeos/icaffeos/frontend_source/public/cafe-images/`

### שלבי העלאה ידנית:
1. **העבר את הקבצים** לתיקייה זו בשרת המק מיני (באמצעות חיבור דיסק-און-קי, רשת מקומית או פקודת `scp`/`rsync`).
2. **פורמט מומלץ לשמות הקבצים:** השתמש באותיות באנגלית ללא רווחים (לדוגמה: `iced_coffee_3330.jpg` או `croissant.png`).
3. לאחר ההעלאה, התמונה תהיה זמינה ברשת המקומית בכתובת:
   🌐 `http://192.168.1.10:8082/cafe-images/שם_הקובץ.סיומת`
4. **עדכון בבסיס הנתונים:**
   במסך הניהול או ישירות בבסיס הנתונים, הגדר את ה-`image_url` של הפריט לכתובת המלאה שנוצרה (או לנתיב היחסי `/cafe-images/שם_הקובץ.סיומת`).

---

## ⚡ כללי זהב לשמירה על מהירות ואופטימיזציית זיכרון

כדי למנוע עומס על זיכרון הטאבלטים והדפדפנים בעסק, הקפד על הכללים הבאים לפני העלאת תמונה כלשהי:

### 📐 מימדים ורזולוציה
* **רזולוציה מקסימלית:** `512x512` פיקסלים (או לכל היותר `800x800`). 
* **יחס גובה-רוחב:** `1:1` (ריבוע מושלם) כדי שהתמונה לא תימתח או תיחתך במסכי ה-POS והקופה.

### 💾 גודל הקובץ (משקל)
* **משקל מומלץ:** מתחת ל-**150KB** לתמונה.
* **פורמט קובץ:** השתמש ב-**WebP** או **JPEG** דחוס. הימנע מקבצי PNG כבדים ולא דחוסים.
* **כלי מומלץ לכיווץ מהיר:** ניתן להשתמש באתרים חינמיים כמו [TinyPNG](https://tinypng.com/) כדי להקטין את משקל הקובץ ב-80% ללא פגיעה באיכות הנראות בטאבלט.

### 🎨 סגנון עיצובי
* **רקע:** מומלץ להשתמש ברקע סטודיו לבן נקי וחלק (או שקוף) לכל פריטי התפריט כדי ליצור קו עיצובי אחיד, פרימיום ונקי במסכים.
