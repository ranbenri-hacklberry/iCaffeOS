# ── Frontend env vars (must be prefixed with VITE_) ──────────────────
# Copy to .env and fill in your values.

# URL of the running Cortex Gateway FastAPI service
VITE_CORTEX_API_URL=http://localhost:8000
